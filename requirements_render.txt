# Minimal requirements for the Render API server deployment.
#
# sentence-transformers, torch, numpy, and pandas have been removed:
# the ranking engine now uses an LLM-only pipeline (full catalog sent to
# GPT-4 in a single call) so no local embedding model is needed.
# This reduces the runtime memory footprint from ~450 MB to ~150 MB,
# comfortably within Render's 512 MB free tier.

# --- LLM / AI ---
openai
python-dotenv

# --- CP-SAT solver ---
ortools

# --- Data bridge (sqlite adapter imports calendar_scraper → beautifulsoup4) ---
beautifulsoup4
requests

# --- API server ---
fastapi>=0.109.0
uvicorn>=0.24.0
pydantic>=2.7.4
python-multipart>=0.0.6
