# Minimal requirements for the Render API server deployment.
# Heavy data-pipeline deps (langchain, scikit-learn, scipy, openpyxl, odfpy,
# rapidfuzz) are intentionally excluded — they are never imported at runtime by
# api_server.py and would push memory past Render's 512 MB free-tier limit.

# --- Core ML / RAG ---
numpy
pandas
sentence-transformers
openai
python-dotenv

# --- CP-SAT solver ---
ortools

# --- Data bridge (sqlite adapter imports calendar_scraper → beautifulsoup4) ---
beautifulsoup4
requests

# --- API server ---
fastapi>=0.109.0
uvicorn>=0.24.0
pydantic>=2.7.4
python-multipart>=0.0.6
